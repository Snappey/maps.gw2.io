/**
 * Polyfills loaded before the app. Targets evergreen browsers.
 *
 * To opt out of parts of the zone.js patch, set the flags below in a separate
 * zone-flags.ts imported before zone.js (webpack hoists imports to the top):
 *   (window as any).__Zone_disable_requestAnimationFrame = true;
 *   (window as any).__Zone_disable_on_property = true; // onclick etc.
 *   (window as any).__zone_symbol__UNPATCHED_EVENTS = ['scroll', 'mousemove'];
 */

import 'zone.js'; // Included with Angular CLI.
